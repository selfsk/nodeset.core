 ! verify all interfaces according implementations
 * unit test for core functionality
 ! make Node as subclass of Service (with startService/stopService), to avoid node.tub.setServiceParent() and
   to make handling of application options more reliable
 ! routing messages between hosts (multiple dispatchers) (via XMPP)
 * weighted subscriptions

